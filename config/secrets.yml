# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 3eb883c53473ff807363d95a399bac933b58789b96f8bb4dd17b379269270bdaa7e80fc5b5c58ec598288b3e67fbe405a1534d96defc1f7c5abc7e7d99c68e13
  twilio_account_sid: AC7a348058356df5d8bd176b3d3c6ca22a #<%= ENV["TWILIO_ACCOUNT_SID"] %>
  twilio_auth_token: 207a87e8eab405fe0f4e2292f86e0205  #<%= ENV["TWILIO_AUTH_TOKEN"] %>
test:
  secret_key_base: 0ea87a5294cf1932fffd5465a40ea7b94e8b025d4522a5a0e52c619cabf72aac5dc42c3a84e307a6f8c89da20f71a308de8b0aa347078d358a0e9d59d0a4874e

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  twilio_account_sid: <%= ENV["TWILIO_ACCOUNT_SID"] %>
  twilio_auth_token: <%= ENV["TWILIO_AUTH_TOKEN"] %>
